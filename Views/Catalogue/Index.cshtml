@model IEnumerable<AppStock.Models.ArticleEntity>
@{
    ViewData["Title"] = "Index";
}
@inject AppStock.Infrastructure.Services.Article.IArticleService Service
@using static AppStock.Models.ArticleEntity

<table class="table">
    <thead>
        <tr>
            <th>
                Libellé
            </th>
            <th>
                Prix Unit.
            </th>
            <th>
                TVA
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Libelle)
                - @Html.DisplayFor(modelItem => item.ArticleFamille.Libelle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrixUnitaire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomTypeTVA.Libelle)
            </td>
            <td>
                <a class="nav-link text-dark" asp-area="" asp-controller="Commande" asp-action="AddArticleToPanier" asp-route-id="@item.Id" >Ajouter au Panier</a>
            </td>
        </tr>
}
    </tbody>
</table>